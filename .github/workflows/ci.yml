name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  check:
    name: Check & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Pinned: a bare `nightly` lets rustfmt and clippy change under the repo,
      # turning an untouched tree red with no code change. This is the last
      # nightly whose `cargo fmt --check` was verified green here (2026-08-25).
      # Bump deliberately, together with the reformat it demands.
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-08-24
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2

      # hakimi-desktop --features gui (pulled by --all-features) needs WebKit 4.1
      - name: Install Tauri / WebKit deps
        run: |
          sudo apt-get update
          # Ubuntu 24.04: ayatana replaces libappindicator3 (packages conflict)
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-08-24

      - uses: Swatinem/rust-cache@v2

      - name: Install Tauri / WebKit deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev

      - name: Run tests
        run: cargo test --workspace --all-features
